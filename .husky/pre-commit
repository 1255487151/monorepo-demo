#!/bin/sh
. "$(dirname -- "$0")/_/husky.sh"

# 执行 lint-staged（静默模式）
pnpm exec lint-staged --relative --quiet

# 检查执行结果
if [ $? -ne 0 ]; then
  echo ""
  echo "❌ Pre-commit checks failed!"
  echo "Please fix the linting errors and try again."
  echo ""
  exit 1
fi