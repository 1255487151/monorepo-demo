import { resolve } from "path"

export const rootDir = resolve(__dirname, "..")
export const srcDir = resolve(rootDir, "src")
export const componentsDir = resolve(srcDir, "components")
export const styleDir = resolve(srcDir, "style")
